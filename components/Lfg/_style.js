import { makeStyles } from "@mui/styles";

const useStyles = makeStyles({
  actionButtons: {
    marginTop: 20,
  },
  platforms: {
    width: 50,
  },
  description: {
    width: "100%",
    height: "100%",
  },
  lfgCard: {
    maxWidth: 800,
    width: "100%",
  },
});

export default useStyles;
