import { makeStyles } from "@mui/styles";

const useStyles = makeStyles({
  platforms: {
    width: 50,
  },
  description: {
    width: "100%",
    height: "100%",
  },
  lfgCard: {
    maxWidth: 800,
    width: "100%",
  },
  actionButtons: {
    marginTop: 20,
  },
});

export default useStyles;
