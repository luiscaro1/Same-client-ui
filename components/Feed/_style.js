import { makeStyles } from "@mui/styles";

const useStyles = makeStyles({
  actionButtons: {
    marginTop: 20,
  },
  platforms: {
    width: 50,
  },
  description: {
    width: "100%",
    height: "100%",
  },
  input: {
    opacity: 0,
    position: "absolute",
    cursor: "pointer",
    width: 95,
    height: 36,
  },
});

export default useStyles;
